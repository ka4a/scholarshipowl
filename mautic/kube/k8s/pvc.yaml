kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: sunrise-mautic-pvc
  namespace: sunrise-mautic
spec:
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: "100Gi"
